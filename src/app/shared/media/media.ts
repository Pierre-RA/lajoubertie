export class Media {
  guid: {
    rendered: string
  };
  title: {
    rendered: string
  };
  media_details: {
    sizes: {
      medium: {
        source_url: string
      },
      medium_large: {
        source_url: string
      },
      full: {
        source_url: string
      }
    }
  };
  description: {
    rendered: string
  };
}
